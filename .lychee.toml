# Limit concurrent requests per host to avoid 429
max_concurrency = 10
host_concurrency = 2
host_request_interval = "1s"

# Retry settings
max_retries = 5
retry_wait_time = 10

[hosts."github.com"]
concurrency = 1
request_interval = "2s"
